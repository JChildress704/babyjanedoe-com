# reference/

Source assets kept here for easy re-editing — not served on the live site
(excluded from deploy in `.github/workflows/deploy.yml`).

- `image_preview.jpg` — full source photo behind `images/og-image.jpg` (the
  link-preview card). To regenerate the card after swapping this file for a
  different photo, see `reference/build-og-image.py`.
